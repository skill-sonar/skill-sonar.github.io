# Production backend base URL (no trailing slash)
# For local dev: leave empty so Vite proxy handles /api/* -> localhost:3002
# For GitHub Actions: set VITE_API_BASE_URL in Settings → Secrets and variables → Actions → Variables
VITE_API_BASE_URL=https://your-backend.example.com
